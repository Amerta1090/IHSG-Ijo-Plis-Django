name = "ihsg-predictor"
main = "src/worker.js"
compatibility_date = "2026-06-01"

# Reverse proxy: passes all requests to your Django backend.
# Django serves static files via WhiteNoise (already configured).
# Set PROXY_ORIGIN in your Cloudflare Worker dashboard or wrangler secret.
# Examples:
#   PROXY_ORIGIN = "https://your-app.railway.app"
#   PROXY_ORIGIN = "https://your-app.fly.dev"
